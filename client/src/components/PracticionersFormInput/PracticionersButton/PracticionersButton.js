import React from 'react';

const PracticionersButton = (props) => (
    <button onClick={props.clicked} className="btn btn-primary">
    Practicioners
    </button>
);

export default PracticionersButton;