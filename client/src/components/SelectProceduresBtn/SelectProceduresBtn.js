import React from "react";
import "./SelectProceduressBtn.css";

const SelectProceduresBtn = props => (
  <span className="reports-btn" {...props}>
    Reports
  </span>
);

export default SelectProceduresBtn;
